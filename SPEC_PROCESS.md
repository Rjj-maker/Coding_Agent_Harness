# SPEC_PROCESS.md — 规约与计划生成过程

> 记录与 Superpowers 框架（OpenCode + DeepSeek V4 Pro）协作生成 SPEC 与 PLAN 的完整过程。

---

## 一、brainstorming 关键节点

### 1.1 智能体追问的关键问题

智能体（brainstorming 技能驱动）在设计中逐次追问了以下问题，每个问题都推动了设计细化：

| 序号 | 问题 | 我的回答 | 对设计的影响 |
|------|------|----------|-------------|
| 1 | 编程语言？ | TypeScript | 确定了技术栈方向，npm 生态、与 Superpowers 同栈 |
| 2 | 主维度（深度方向）？ | 反馈闭环 | 决定了 harness 的核心贡献点 |
| 3 | 工具集范围？ | 标准集（读/写/shell/测试/lint/搜索） | 确定了 agent 的行动能力边界 |
| 4 | 分发形态？ | npm 包 | 确定了分发策略和 CI 构建产物 |
| 5 | LLM 供应商策略？ | 多供应商，默认 NJUSE Hub | 影响了 LLM 抽象层的设计 |
| 6 | CLI 交互模式？ | REPL + 单次任务 | 决定了 CLI 入口的两种模式 |

### 1.2 让我修正了原设想的关键问题

**最重要的修正：主维度选择。**

智能体最初推荐"治理（护栏/沙箱/HITL）"作为主维度，理由充分：危险命令匹配引擎、HITL 状态机、沙箱隔离等都有充足的工程深度空间。但我追问"有比较好实现的方向吗？"后，智能体改推荐"反馈闭环"，理由更务实：

- 逻辑简单直接（解析 exitCode → 分类 → 摘要 → 回灌）
- Mock 测试天然友好（只需 mock 命令执行结果）
- 与 Coding 场景高度契合（exitCode 0 = 通过，非 0 = 失败）
- 深入空间也不小（失败分类、多轮修正、上下文摘要）

**这个修正对我很重要**——作为第一次做 harness 项目，选择一个"好实现"的主维度比"深度空间最大"的更实际。反馈闭环的每一行代码都是确定性的，这完美契合了"机制必须是代码，不能是提示词"的硬要求。

---

## 二、至少 3 轮关键迭代

### 迭代 1：主维度从"治理"到"反馈闭环"

**智能体建议**：治理作为主维度（护栏/沙箱/HITL），理由是"最契合机制必须是代码的硬要求"。

**我的处理**：追问"有比较好实现的方向吗？"，最终选择了反馈闭环。

**决策理由**：反馈闭环的 mock 测试路径更短、更清晰。治理的 HITL 状态机需要交互式测试，跨越了"确定性单元测试"的边界。

### 迭代 2：去掉 WebUI 和线上部署

**智能体建议**：WebUI 通过浏览器与 harness 交互，部署到 Vercel/Render。

**我的处理**：经与助教确认，只需 CLI 即可，去掉了 WebUI 和线上部署。

**决策理由**：作业的核心要求是 harness 内核的工程深度，WebUI 会分散精力。聚焦 CLI 让整个项目更紧凑。

### 迭代 3：设计块的逐块确认

**智能体方式**：将系统架构、反馈闭环、治理护栏、数据模型、凭据与分发、非功能需求等分块呈现，逐块让我确认。

**我的处理**：每个块逐一审查，确认数据模型中的类型定义、反馈闭环的流水线设计、护栏的黑名单匹配策略等。

**决策理由**：这种方式让我能逐块把关，而不是一次性面对整个复杂设计。系统架构确认时我说"不需要"调整，因为简洁清晰；数据模型确认时所有类型都对应 SPEC 中的功能规约。

---

## 三、AI 建议的采纳与修正

### 采纳的 AI 建议

| 建议 | 来源 | 采纳理由 |
|------|------|----------|
| TypeScript 作为编程语言 | brainstorming 初次提问 | 与 Superpowers 同栈，生态丰富 |
| 反馈闭环作为主维度 | 第二次推荐 | 实现路径清晰，mock 测试友好 |
| 标准工具集（6 个工具） | brainstorming 第三次提问 | 覆盖 coding agent 核心能力，不多不少 |
| npm 作为分发形态 | brainstorming 第四次提问 | TypeScript 项目天然选择 |
| 多供应商 LLM 抽象层 | brainstorming 第五次提问 | 灵活可扩展，OpenAI 兼容格式 |
| REPL + 单次任务双模式 | brainstorming 第六次提问 | 开发成本差异不大，覆盖两种场景 |
| 系统架构图（CLI → Loop → Tools/Guardrail/Feedback/Memory） | 设计呈现阶段 | 清晰的分层结构 |
| 反馈闭环的流水线设计（解析 → 分类 → 摘要 → 回灌） | 设计呈现阶段 | 每一步都是确定性逻辑 |
| 治理护栏的黑名单 + 白名单设计 | 设计呈现阶段 | 简单有效，纯正则匹配 |
| 数据模型中的类型定义 | 设计呈现阶段 | 完整的 TypeScript 接口定义 |

### 我推翻或修正的 AI 建议

| 原建议 | 我的修正 | 理由 |
|--------|----------|------|
| 治理作为主维度 | 改为反馈闭环 | 更容易实现，mock 测试路径更短 |
| WebUI + 线上部署 | 去掉 | 经助教确认只需 CLI |
| 默认 LLM 为 DeepSeek V4 Pro | 改为 DeepSeek V3 | 更稳定，成本更低，开发阶段够用 |

---

## 四、反思：brainstorming 技能的评价

### 做得好的地方

1. **逐块确认机制**：不是一次性抛出整个设计，而是分块呈现、逐块确认。这让我能在每个决策点停下来思考，而不是被淹没在一大堆设计细节中。

2. **追问精炼**：每个问题都有明确的选项和推荐，不是开放式提问。例如"编程语言？"给了 4 个选项并标注推荐理由，这比"你想用什么语言？"高效得多。

3. **设计块之间逻辑清晰**：架构图 → 反馈闭环 → 治理护栏 → 数据模型 → 凭据 → 非功能需求，这个顺序从宏观到微观，从核心到外围，符合认知规律。

4. **推荐有理由**：每个推荐都附带了 WHY，让我能基于理由做判断，而不是盲从。

### 让我不满的地方

1. **设计块之间的关联性说明不足**：例如反馈闭环和治理护栏之间如何交互，在单独呈现时没有明确说明。我在 SPEC 中需要自己补充数据流和组件关系。

2. **缺少对"最困难部分"的预警**：brainstorming 没有主动指出"反馈闭环的失败分类准确性"是潜在风险点——正则匹配可能误判。这是我在 SPEC 的"风险与未决问题"中自己补充的。

3. **对作业要求的某些细节不够敏感**：例如最初建议 WebUI + 线上部署，但作业的通用要求中 WebUI 是"必须提供"的，这部分是我自己纠正的（虽然最终确认不需要）。如果 brainstorming 能更主动地对照作业要求检查，会更好。

4. **设计块之间的确认节奏有时过快**：有些设计块（如数据模型）我确认后没有深入讨论扩展点，brainstorming 直接进入了下一个块。如果能在每个关键块确认后追问一句"这个设计有没有你觉得不够的地方？"，会更有助于发现隐藏假设。

---

## 五、writing-plans 阶段的记录

### 产出过程

PLAN.md 由 writing-plans 技能驱动生成。智能体首先确定了文件结构（`src/` 下按模块拆分为 config/llm/agent/tools/guardrail/feedback/memory/utils/），然后按依赖关系分解为 11 个 Task。

### 关键特征

- 每个 Task 包含完整的 TDD 流程：先写失败测试 → 验证红色 → 写实现 → 绿色 → commit
- Task 3/4/5/6 可并行（都只依赖 Task 2）
- Task 10/11 可并行（无依赖）
- 所有代码块是具体实现而非占位符

### 对 PLAN 的反思

writing-plans 产出的 PLAN 非常详细，但也存在一个问题：**Plan 中包含了完整的实现代码，这可能导致 subagent 在实现时直接"抄写"而非理解后实现**。我会在 subagent 派发时注意这一点，要求 subagent 理解设计意图后再编码。

---

## 六、与 Superpowers 协作的总体感受

这次 brainstorming + writing-plans 的协作体验，让我对"与 AI 共同设计"有了更具体的理解：

1. **AI 是加速器，不是替代品**：AI 能快速产出结构化的设计文档，但设计决策需要我基于对作业要求的理解来把关。WebUI 的去掉、主维度的修正，都是 AI 无法替我做判断的地方。

2. **逐块确认是好的设计审查机制**：每次只聚焦一个设计块，减少了认知负荷，提高了审查质量。

3. **SPEC 的质量取决于追问的质量**：brainstorming 追问了 6 个关键问题，每个问题都让设计更具体。如果跳过这个阶段直接写 SPEC，很多细节会含糊不清。

4. **"隐藏假设"是最危险的**：冷启动验证（§4.5）的设计正是为了暴露这些假设——我和主 agent 在 brainstorming 中沉淀了大量共享上下文，这些上下文没有明文写入 SPEC，会让另一个 agent 受阻。下一步的冷启动验证将是最关键的考验。