# Coding Agent Harness 项目反思报告

> 项目类型：A · Coding Agent Harness ｜ 作者：饶嘉俊（241880617）｜ 日期：2026-08-13

## 一、项目概述

本项目实现了一个 TypeScript 编写的 Coding Agent Harness——LLM 与文件系统之间的工程化中间层，主循环、工具分发、治理护栏、反馈闭环四类核心机制全部由确定性代码实现，可在 Mock LLM 下独立验证。开发按 Superpowers 框架推进，交付 36 个测试、6 个工具、CLI 双模式和 npm 包。下面按课程要求逐条反思。

## 二、Superpowers 技能：哪些发挥了最大作用，哪些形式大于实质

**发挥最大作用的：**

1. **subagent-driven-development。** 11 个 Task 全部由 general subagent（DeepSeek V4 Pro）独立完成，AGENT_LOG 里 11 条记录全是"人工干预：无"，我实际只做了两件事：写好 task brief、检查 commit 和测试结果。
2. **brainstorming 的追问机制。** 6 个带选项和理由的追问把设计逐步钉死。影响最大的是主维度修正：智能体最初推荐"治理"，我追问"有没有更好实现的方向？"后改推荐"反馈闭环"——它每一行都是确定性数据转换，Mock 测试路径极短。
3. **冷启动验证。** 让 Claude Code 从零执行 Task 1+2，暴露 5 个缺陷（其中 3 个是 SPEC/PLAN 数值矛盾）。

**形式大于实质的：**

- **writing-plans 的"完整代码"模式。** PLAN 有 2300 多行，每个 Task 附完整实现代码，看似严谨，实际让 subagent 变成抄写员——代码全绿，但我无法知道它有没有理解设计意图。
- **TDD 的红色阶段在纯函数模块上偏仪式化**，它的价值不在红色，而在逼我把验收标准写成代码。

## 三、TDD 在 AI 协作下：阻碍还是放大器？

**结论：核心机制上是放大器；但边界之外，项目质量迅速下滑。**

每个 task brief 附带先写失败测试的要求，"完成"被定义为"红色 → 绿色 + commit"，subagent 不用理解我的意图，只要让测试变绿。MockLLMProvider 让我能断言"第二轮 LLM 上下文里包含 failed 反馈"这种原本不可观察的行为，36 个测试最终成为验收标准。

但 TDD 只护住了核心机制。CLI 交互层（Task 8 之后）没有测试护航——实现阶段之后的二十几个提交，一大半在反复修界面显示、Ctrl+C、"回答不显示"这类写不进单元测试的问题。

**体会：TDD 是阻碍还是放大器，取决于验收标准能不能写成测试。**

## 四、subagent 能自主运行多久？什么 task 颗粒度最优？

**自主时长：一个 Task 的量级。** 有 brief、测试验收和 commit 的约束，subagent 从没偏离过——11 个 task 没有一个需要中途干预。但离开约束，偏离马上发生：实现结束后主智能体进入"改进界面"的开放式阶段，十几个提交在界面细节上反复打转。**没有验收标准的自由发挥，谁都会漂移。**

**最优颗粒度：一个模块 + 一套能跑的测试。** Task 4（工具系统）最大——10 个文件、6 个工具——却是返工最少的，因为 Tool 接口在 PLAN 里钉死，验收标准完全客观；Task 8（CLI + 凭据）只有 4 个文件，却是返工最多的，因为验收标准涉及用户体验和跨平台，写不进测试。**颗粒度的标准不是文件数，而是"验收标准能否完全客观化"。**

## 五、SPEC / PLAN 质量如何影响实现质量

**SPEC 质量的决定因素不是写得多详细，而是 SPEC 与 PLAN 有没有矛盾、边界条件有没有闭合、验收标准能不能测。** 冷启动验证暴露的数值矛盾就是例子：SPEC 写记忆保留"最近 10 轮对话"、PLAN 写 maxMessages: 50。若留到实现阶段，subagent 按 PLAN 写的代码会和 SPEC 验收标准冲突。

**规约不清导致 subagent 偏离的具体案例：HITL 确认流。** SPEC §3.6 只写了一句"HITL 确认超时 30s 自动拒绝"，没定义交互协议：谁等待谁、确认结果怎么注入主循环。Task 5 的 subagent 按字面做了最小实现——need_approval 成了终止状态：危险命令被拦截后 agent 直接停机，而不是进入"等待确认 → 超时拒绝 → 继续"的循环。规约的一句话被解读成了最省事的解释，事后修复等于重做 Task 5 的一部分。

**教训：规约里每句话都要有对应的实现路径和数据流；规约越短，subagent 自由发挥的空间越大。**

## 六、最有效的 prompt / context 策略

1. **把验收标准写进 task brief。** brief 包含接口定义、要写的测试和 commit 要求，"完成"有了客观定义。11 个 task 零干预，靠的不是智能体聪明，而是 brief 没有歧义可钻。
2. **用 Mock 预设响应把"LLM 看到了什么"变成可断言的数据。** 上下文质量本是 agent 项目里最难验证的东西，这个设计把它变成了普通断言。
3. **跨智能体冷启动。** 把文档交给另一个 agent 从零执行，才能暴露我们沉淀的隐性共识——同一个 agent 永远发现不了自己"以为写清楚了"。

## 七、凭据与分发：两条工程要求迫使我想清楚的问题

以前写课程作业，API key 直接写死在代码里。这两条要求第一次逼我站到"攻击者"和"使用者"的视角审查自己的项目。

**凭据：** 第一次认真列威胁模型：key 不能出现在源码、Git 历史、终端日志、运行机器的普通文件中；set-key 必须隐藏回显；.env 备选是明文，要在 README 警告并加入 .gitignore。

**分发：** 为什么 npm install -g 之后就有命令可用？因为 package.json 的 bin、main、engines 字段，以前我从没看过；分发的是构建产物 dist/ 而非源码；CI 发布用的 npm token 本身又是一种凭据，要放 GitHub Secrets。

**总结：这两条要求逼我完成了视角转换——从"能跑就行"变成"别人怎么用、别人怎么攻击"。**

## 八、如果重做，我会改变什么

1. **SPEC 阶段就定终端输出格式**（Markdown 渲染器是后期补的）。
2. **PLAN 里有意识地留白。** 只给接口和测试，让 subagent 自己填充实现，验证理解能力而非抄写能力。
3. **HITL 抽象为可注入的 ApprovalProvider 接口**，用 Mock 测试交互确认，而不是把 need_approval 做成终止状态。
4. **补多轮修正的集成测试**（Demo 2 只覆盖了一轮修正，二轮路径没测）。
5. **Memory 按 token 截断；给交互层写人工冒烟清单**，把试出来的问题变成验收步骤。

## 九、对 Superpowers 方法论的批判：它假设了什么？

1. **假设用户是合格的设计审查者。** 逐块确认假定每块我都能判断好坏，实际对数据模型这类块我是"看起来对"就过了。**只部分成立。**
2. **假设 PLAN 越详细实现越忠实。** 实际把 subagent 变成抄写员，理解深度无法验证。**不成立。**
3. **假设任务可清晰分解为依赖 DAG。** 核心模块成立；CLI、交互、跨平台部分不成立，而这正是返工集中地。**核心成立，外围不成立。**
4. **假设 TDD 覆盖质量。** 体验类质量测不了，TDD 盲区就是 bug 来源。**不成立。**
5. **假设 LLM 稳定到足以独立执行。** task 内稳定（11/11 零干预）；开放式阶段它的判断不可控。**task 内成立，task 外不成立。**
6. **"机制必须是代码"的价值观假设。** 这条成立，也是我认同的、方法论里最有价值的部分。

**总结：Superpowers 在"可规格化的核心机制"上成立，在"体验、交互、跨平台"上不成立——最顺利的部分正好是假设成立的部分。**

## 十、学术规范声明

1. **手写代码声明**：本项目未手写核心算法——主循环、工具、护栏、反馈闭环、记忆模块均按 PLAN.md 规约由 subagent 在 TDD 流程下实现，无手写改写；后续修改均有 git 提交记录可查。
2. **第三方代码与许可证**：使用 Commander.js（MIT）、OpenAI SDK（Apache-2.0）、keytar（MIT）、Vitest（MIT）等库，均遵守其许可证；项目自身为 MIT，第三方依赖及许可证已列于 README。
3. **AI 使用声明**：本反思报告由学生本人撰写，成稿阶段使用 AI 辅助润色与结构整理（按要求在此标注）。开发过程中 AI 的使用已完整记录于 SPEC_PROCESS.md 与 AGENT_LOG.md。
